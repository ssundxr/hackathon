services:
  cvanalyzer:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:8000"
      - "8080:8000"
    environment:
      - ENV=staging
      - DEBUG=true
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: always
