services:
  cvanalyzer:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:8000"
    environment:
      - ENV=production
      - DEBUG=false
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: always
